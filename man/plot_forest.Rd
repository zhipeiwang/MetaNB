% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forest.R
\name{plot_forest}
\alias{plot_forest}
\title{Forest plot for trivariate meta-analysis summaries}
\usage{
plot_forest(
  samples,
  data,
  label_cols = NULL,
  study_label_col = NULL,
  metric = c("NB", "RU", "sens", "spec"),
  center = c("Median", "Mean"),
  t = NULL,
  xlim = NULL,
  xticks = NULL,
  plot_col_width = 50,
  reported_low_col = NULL,
  reported_high_col = NULL,
  reported_est_col = NULL,
  interval_fallback = NULL,
  mark_imputed = TRUE,
  file_png = NULL,
  file_pdf = NULL
)
}
\arguments{
\item{samples}{A `coda::mcmc.list` of posterior samples, typically
`fit$samples` from [MA_NB_tri()].}

\item{data}{A data frame containing study-level data and labels.}

\item{label_cols}{Character vector of columns in `data` to display in the
plot table.}

\item{study_label_col}{Optional column in `data` used for study labels and
summary row labels. If `NULL`, the first entry of `label_cols` is used.}

\item{metric}{Metric to plot. One of `"NB"`, `"RU"`, `"sens"`, or `"spec"`.}

\item{center}{Summary measure used for pooled point estimates. One of
`"Median"` or `"Mean"`.}

\item{t}{Threshold probability. Required when observed-study net benefit or
analytical net benefit confidence intervals are used.}

\item{xlim}{Optional x-axis limits.}

\item{xticks}{Optional tick marks for the x-axis.}

\item{plot_col_width}{Width of the blank plotting column used internally by
`forestploter`.}

\item{reported_low_col}{Optional column in `data` containing reported lower
interval bounds.}

\item{reported_high_col}{Optional column in `data` containing reported upper
interval bounds.}

\item{reported_est_col}{Optional column in `data` containing reported point
estimates.}

\item{interval_fallback}{Optional fallback interval method. If `NULL`, a
default is chosen based on `metric`.}

\item{mark_imputed}{Logical; whether to mark study rows where displayed point
estimates or intervals were filled in rather than fully taken from
reported study values.}

\item{file_png}{Optional file path for saving the plot as PNG.}

\item{file_pdf}{Optional file path for saving the plot as PDF.}
}
\value{
A forest plot object produced by the `forestploter` package. The plot is also
drawn as a side effect.
}
\description{
Creates a forest plot for net benefit, relative utility, sensitivity, or
specificity using posterior samples from a Bayesian trivariate meta-analysis
and the corresponding study-level data.
}
\examples{
\dontrun{
fit <- MA_NB_tri(data, tp = tp, tn = tn,
                 n_event = n_event, n_nonevent = n_nonevent,
                 t = 0.1, prior_type = "weak", seed = 123)
plot_forest(samples = fit$samples, data = data, metric = "NB",
            label_cols = c("Publication", "Country", "N", "Prev"),
            t = 0.1)
}
}
